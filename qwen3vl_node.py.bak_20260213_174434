"""
Qwen3-VL Uncensored - ComfyUI Node
===================================
Der eigentliche ComfyUI Custom Node

Autor: Martin + Claude Coach
Datum: 2026-02-13
"""

import torch
import numpy as np
from PIL import Image
import tempfile
from pathlib import Path

PRESET_PROMPTS = [
    "🖼️ Tags",
    "🖼️ Simple Description",
    "🖼️ Detailed Description",
    "🖼️ Ultra Detailed Description",
    "🎬 Cinematic Description",
    "🖼️ Detailed Analysis",
    "📹 Video Summary",
    "📖 Short Story",
    "🪄 Prompt Refine & Expand",
]


from .llama_wrapper import LlamaWrapper


class Qwen3VLPromptGenerator:
    """
    ComfyUI Node: Qwen3-VL Prompt Generator (Uncensored)
    
    Modes:
    - enhance:    Text → Enhanced Text
    - describe:   Image → Prompt
    - multimodal: Text + Image → Enhanced Prompt
    """
    
    def __init__(self):
        self.llama = None  # Lazy init (erst bei erster Nutzung)
    
    @classmethod
    def INPUT_TYPES(cls):
        """
        Definiert die Input-Ports des Nodes
        
        ComfyUI zeigt diese in der UI an
        """
        return {
            "required": {
                "mode": (["enhance", "describe", "multimodal"], {
                    "default": "enhance"
                }),
            "preset_prompt": (PRESET_PROMPTS, {"default": "🖼️ Detailed Analysis"}),
            },
            "optional": {
                "text": ("STRING", {
                    "multiline": True,
                    "default": "",
                    "placeholder": "Enter text prompt to enhance..."
                }),
                "image": ("IMAGE",),  # ComfyUI Bild-Tensor
                "temperature": ("FLOAT", {
                    "default": 0.7,
                    "min": 0.0,
                    "max": 1.0,
                    "step": 0.05
                }),
                "max_tokens": ("INT", {
                    "default": 400,
                    "min": 50,
                    "max": 1000,
                    "step": 50
                }),
            "use_preset_tokens": ("BOOLEAN", {"default": True}),
            }
        }
    
    RETURN_TYPES = ("STRING",)
    RETURN_NAMES = ("generated_prompt",)
    FUNCTION = "generate_prompt"
    CATEGORY = "prompt/qwen3vl"
    
    def generate_prompt(
        self, 
        mode: str,
        text: str = "",
        image = None,
        temperature: float = 0.7,
        max_tokens: int = 400
    ):
        """
        Haupt-Funktion des Nodes
        
        Args:
            mode: "enhance" / "describe" / "multimodal"
            text: Optional text prompt
            image: Optional ComfyUI image tensor
            temperature: Kreativität
            max_tokens: Max Antwort-Länge
            
        Returns:
            Tuple mit (generated_prompt_string,)
        """
        
        # Lazy init des LlamaWrapper
        if self.llama is None:
            try:
                self.llama = LlamaWrapper()
                print("[Qwen3-VL] ✅ LlamaWrapper initialisiert")
            except Exception as e:
                error_msg = f"❌ ERROR: LlamaWrapper init failed:\n{str(e)}"
                print(f"[Qwen3-VL] {error_msg}")
                return (error_msg,)
        
        # Update config mit User-Parametern
        from . import config
        config.TEMPERATURE = temperature
        config.MAX_TOKENS = max_tokens
        
        # Mode-Routing
        try:
            if mode == "enhance":
                result = self._mode_enhance(text)
            elif mode == "describe":
                result = self._mode_describe(image)
            elif mode == "multimodal":
                result = self._mode_multimodal(text, image)
            else:
                result = f"❌ ERROR: Unknown mode '{mode}'"
            
            print(f"[Qwen3-VL] Generated prompt ({len(result)} chars)")
            return (result,)
            
        except Exception as e:
            error_msg = f"❌ ERROR in {mode} mode:\n{str(e)}"
            print(f"[Qwen3-VL] {error_msg}")
            return (error_msg,)
    
    def _mode_enhance(self, text: str) -> str:
        """Mode: enhance - Text → Enhanced Text"""
        if not text or not text.strip():
            return "❌ ERROR: Text input required for 'enhance' mode!"
        
        print(f"[Qwen3-VL] Enhancing text: '{text[:50]}...'")
        return self.llama.enhance_prompt(text)
    
    def _mode_describe(self, image) -> str:
        """Mode: describe - Image → Prompt"""
        if image is None:
            return "❌ ERROR: Image input required for 'describe' mode!"
        
        # Konvertiere ComfyUI Tensor → PIL Image → temporäre Datei
        image_path = self._tensor_to_tempfile(image)
        
        print(f"[Qwen3-VL] Describing image: {image_path}")
        result = self.llama.describe_image(image_path)
        
        # Cleanup
        image_path.unlink()
        
        return result
    
    def _mode_multimodal(self, text: str, image) -> str:
        """Mode: multimodal - Text + Image → Enhanced Prompt"""
        if not text or not text.strip():
            return "❌ ERROR: Text input required for 'multimodal' mode!"
        if image is None:
            return "❌ ERROR: Image input required for 'multimodal' mode!"
        
        # Konvertiere ComfyUI Tensor → PIL Image → temporäre Datei
        image_path = self._tensor_to_tempfile(image)
        
        print(f"[Qwen3-VL] Multimodal: text='{text[:50]}...' + image")
        result = self.llama.multimodal_enhance(text, image_path)
        
        # Cleanup
        image_path.unlink()
        
        return result
    
    def _tensor_to_tempfile(self, image_tensor) -> Path:
        """
        Konvertiert ComfyUI Image-Tensor zu temporärer Bild-Datei
        
        ComfyUI Tensors sind: [batch, height, width, channels]
        Wir nehmen nur das erste Bild im Batch
        """
        
        # Nimm erstes Bild im Batch
        if len(image_tensor.shape) == 4:
            img = image_tensor[0]
        else:
            img = image_tensor
        
        # Konvertiere zu numpy array
        img_np = (img.cpu().numpy() * 255).astype(np.uint8)
        
        # Erstelle PIL Image
        pil_img = Image.fromarray(img_np)
        
        # Speichere in temporäre Datei
        temp_file = tempfile.NamedTemporaryFile(delete=False, suffix='.png')
        temp_path = Path(temp_file.name)
        pil_img.save(temp_path)
        temp_file.close()
        
        return temp_path


# Node-Mapping für ComfyUI
NODE_CLASS_MAPPINGS = {
    "Qwen3VLPromptGenerator": Qwen3VLPromptGenerator
}

NODE_DISPLAY_NAME_MAPPINGS = {
    "Qwen3VLPromptGenerator": "Qwen3-VL Prompt Generator (Uncensored) 🔓"
}
